package main.runners;

import main.utils.Graph;
import main.utils.Path;

/**
 * Tries to extend a path given a graph that it's built on
 * @author Peijin Zhang
 */
public class PathExtender {
  
  /**
   * Extends the given path on a graph and returns the new path
   */
  public static Path extendPath(Graph graph, Path path) {
    return path;
  }
}
