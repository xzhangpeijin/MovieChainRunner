package main;

import java.io.File;

import utils.Graph;

public class SearchPaths 
{
  private final Graph graph;
  
  public SearchPaths(Graph graph) {
    this.graph = graph;
  }
  
  /** 
   * Search never ends
   * Writes results to file 
   */
  public void search(File dir) {
    
  }
}
